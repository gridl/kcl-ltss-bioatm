from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.0.1',
    description='A unet based classifier for extracting smoke plumes from EO images',
    author='dnfisher',
    license='MIT',
)
